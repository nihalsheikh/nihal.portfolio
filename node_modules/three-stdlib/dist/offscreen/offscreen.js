"use strict";
const scene = require("./scene.js");
if (typeof self !== "undefined") {
  self.onmessage = function(message) {
    const data = message.data;
    scene(data.drawingSurface, data.width, data.height, data.pixelRatio, data.path);
  };
}
